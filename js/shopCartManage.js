// Going to make this somehow allow you to add item to session that will allow to make shopping cart work

function AddToCart(name){
    if(sessionStorage.getItem(name) !== null)
        sessionStorage.setItem(name, )
}